<div class="container">
    <div class="row">
        <div class="col-sm">
            <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-toggle="dropdown"
                aria-haspopup="true" aria-expanded="false">View a Bird</button>
            <div class="dropdown-menu">
                {{#each birds}}
                {{>bird-dropdown}}
                {{/each}}
            </div>
        </div>
        <div class="col-sm">
            <button>View All Birds</button>
        </div>
        <div class="col-sm">
            <button>Add a Bird</button>
        </div>
    </div>
    <h1>Recently Spotted:</h1>
    {{>recently-spotted}}
</div>